name: weebi_barcode_scanner_example
description: Example app for weebi_barcode_scanner package

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  weebi_barcode_scanner:
    path: ../
  camera: ^0.10.0
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/ 