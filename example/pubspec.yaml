name: barcode_scanner_example
description: "Example app demonstrating weebi_barcode_scanner_package with OpenFoodFacts integration"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.1
  weebi_barcode_scanner:
    path: ../
  permission_handler: ^11.0.0
  openfoodfacts: ^3.23.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/ 